# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
# OMN-6912

name: PR Title Check

on:
  pull_request:
    types: [opened, edited]

jobs:
  pr-title:
    # self-gating-ok: cross-repo reusable workflow; PR Title Check is not a required status check on main (tracked in OMN-9039)
    uses: OmniNode-ai/onex_change_control/.github/workflows/pr-title-check-reusable.yml@main
    permissions:
      pull-requests: read
