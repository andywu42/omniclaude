---
schema_version: "1.0.0"
ticket_id: OMN-10278
summary: "Unify all session-ID env-var reads onto canonical CLAUDE_CODE_SESSION_ID"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "All CI checks pass on omniclaude PR #1558"
    command: "gh pr checks 1558 --repo OmniNode-ai/omniclaude --watch"
  - kind: "unit"
    description: "Session-ID resolver unit tests and E2E proof-of-life pass"
    command: "uv run pytest tests/unit/test_session_id_canonical_e2e.py tests/unit/hooks/scripts/test_shell_session_id_migration.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Session-ID resolver unit and E2E tests pass"
    source: "ci"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/test_session_id_canonical_e2e.py tests/unit/hooks/scripts/test_shell_session_id_migration.py -v"
  - id: "dod-002"
    description: "All handler_auto_emit runtime paths use canonical session resolver"
    source: "ci"
    checks:
      - check_type: "command"
        check_value: "grep -r 'resolve_session_id\\|CLAUDE_CODE_SESSION_ID' src/omniclaude/nodes/node_agent_chat/handler_auto_emit.py"
  - id: "dod-003"
    description: "Deploy verification: runtime handler migrated to canonical session-ID resolver, no legacy env-var reads remain"
    source: "ci"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 1558 --repo OmniNode-ai/omniclaude --watch # deploy-verify OMN-10278 runtime handler migration"
