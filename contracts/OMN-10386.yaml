---
schema_version: "1.0.0"
ticket_id: OMN-10386
title: "Augment dispatch worker completion payload"
summary: "Augment dispatch_worker-completed.v1 payload with cost and artifact attribution"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Dispatch worker runtime publisher emits cost, artifact, and provenance fields on the contract completion topic"
    command: "PYTHONPATH=$OMNI_HOME/omni_worktrees/OMN-10386/omnibase_core/src uv run pytest tests/unit/runtime/test_wiring_dispatchers.py -q"
  - kind: "manual"
    description: "Runtime publisher and completion event model pass repo lint"
    command: "PYTHONPATH=$OMNI_HOME/omni_worktrees/OMN-10386/omnibase_core/src uv run ruff check src/omniclaude/runtime/wiring_dispatchers.py src/omniclaude/shared/models/model_skill_completion_event.py tests/unit/runtime/test_wiring_dispatchers.py"
  - kind: "manual"
    description: "Runtime publisher and completion event model pass mypy with the OMN-10381 core dependency"
    command: "PYTHONPATH=$OMNI_HOME/omni_worktrees/OMN-10386/omnibase_core/src uv run mypy src/omniclaude/runtime/wiring_dispatchers.py src/omniclaude/shared/models/model_skill_completion_event.py"
  - kind: "manual"
    description: "Post-merge deploy smoke confirms the live dispatch_worker-completed.v1 topic carries the augmented payload"
    command: "after user-authorized deploy to .201, run: docker exec omninode-bridge-redpanda rpk topic consume onex.evt.omniclaude.dispatch_worker-completed.v1 --num 1 --offset end --format '%v' | jq '.token_cost, .dollars_cost, .artifact_path, .model_calls[0].cost_provenance, .cost_provenance'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused runtime tests cover dispatch_worker contract-topic fanout and augmented completion payload fields"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=$OMNI_HOME/omni_worktrees/OMN-10386/omnibase_core/src uv run pytest tests/unit/runtime/test_wiring_dispatchers.py -q"
  - id: "dod-002"
    description: "Runtime publisher and completion event model pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=$OMNI_HOME/omni_worktrees/OMN-10386/omnibase_core/src uv run ruff check src/omniclaude/runtime/wiring_dispatchers.py src/omniclaude/shared/models/model_skill_completion_event.py tests/unit/runtime/test_wiring_dispatchers.py"
  - id: "dod-003"
    description: "Runtime publisher and completion event model pass typecheck against the OMN-10381 core dependency"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=$OMNI_HOME/omni_worktrees/OMN-10386/omnibase_core/src uv run mypy src/omniclaude/runtime/wiring_dispatchers.py src/omniclaude/shared/models/model_skill_completion_event.py"
  - id: "dod-004"
    description: "Post-merge deploy smoke validates the live dispatch_worker-completed.v1 payload includes cost and artifact attribution"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "after user-authorized deploy to .201, run: docker exec omninode-bridge-redpanda rpk topic consume onex.evt.omniclaude.dispatch_worker-completed.v1 --num 1 --offset end --format '%v' | jq '.token_cost, .dollars_cost, .artifact_path, .model_calls[0].cost_provenance, .cost_provenance'"
