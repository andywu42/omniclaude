---
schema_version: "1.0.0"
ticket_id: OMN-10138
summary: "Add ModelCrossCLIInvocationResult for Cross-CLI Bridge subprocess dispatch results"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: "Cross-CLI invocation result model tests cover computed success and defaults"
    command: "uv run pytest tests/unit/nodes/node_delegation_orchestrator/test_model_cross_cli_invocation_result.py -q"
  - kind: ci
    description: "omniclaude PR #1437 CI green across required gates"
    command: "gh pr checks 1437 --repo OmniNode-ai/omniclaude --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Cross-CLI invocation result behavior is covered by focused unit tests"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/nodes/node_delegation_orchestrator/test_model_cross_cli_invocation_result.py -q"
  - id: dod-002
    description: "Runtime-adjacent Cross-CLI result schema change has a deploy verification surface before merge"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/nodes/node_delegation_orchestrator/test_model_cross_cli_invocation_result.py -q  # deploy-verify ModelCrossCLIInvocationResult schema"
