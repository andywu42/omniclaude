---
schema_version: "1.0.0"
ticket_id: OMN-10120
title: "Delete legacy omniclaude publisher source tree"
summary: "Delete legacy omniclaude publisher source tree"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: manual
    description: "Legacy publisher package and tests are absent and no runtime source references the legacy module"
    command: "test ! -d src/omniclaude/publisher && test ! -d tests/publisher && ! rg 'omniclaude\\.publisher' src tests plugins scripts --glob '!uv.lock'"
  - kind: tests
    description: "Runtime lifecycle and launcher cutover tests pass"
    command: "uv run pytest tests/runtime/test_lifecycle.py tests/runtime/test_plugin_claude.py tests/scripts/test_emit_daemon_cutover_static.py tests/scripts/test_omnimarket_launcher.py -q"
  - kind: tests
    description: "Full unit suite passes"
    command: "uv run pytest tests/ -v"
  - kind: ci
    description: "omniclaude PR CI green across required gates"
    command: "gh pr checks --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Legacy publisher source and tests are removed"
    source: manual
    checks:
      - check_type: command
        check_value: "test ! -d src/omniclaude/publisher && test ! -d tests/publisher"
  - id: dod-002
    description: "No runtime source references the deleted legacy module"
    source: manual
    checks:
      - check_type: command
        check_value: "! rg 'omniclaude\\.publisher' src tests plugins scripts --glob '!uv.lock'"
  - id: dod-003
    description: "Runtime lifecycle cutover tests pass before post-merge deploy validation"
    source: manual
    checks:
      - check_type: command
        check_value: "echo 'deploy validation required after merge' && uv run pytest tests/runtime/test_lifecycle.py tests/runtime/test_plugin_claude.py tests/scripts/test_emit_daemon_cutover_static.py tests/scripts/test_omnimarket_launcher.py -q"
