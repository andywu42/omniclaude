---
schema_version: "1.0.0"
ticket_id: "OMN-11599"
title: "Migrate agent_router to node structure"
summary: >
  Adds node_agent_router as a typed COMPUTE_GENERIC wrapper around the legacy AgentRouter while preserving
  the existing hook-compatible router path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-agent-router-unit-tests"
    description: "Agent router node unit tests pass on omniclaude PR #1677 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_agent_router/ -v"
  - id: "dod-agent-router-deploy-gate"
    description: "OMN-11599 touches runtime node paths and requires deploy validation before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "deploy validation required for omniclaude node_agent_router runtime paths"
