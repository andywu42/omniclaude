---
schema_version: "1.0.0"
ticket_id: OMN-11220
title: "Verification-First Parallel Worker Dispatch Skill"
summary: "Build verified_dispatch skill shim and node_verified_dispatch_orchestrator with paired worker/verifier dispatch and bounded escalation"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Golden chain tests for node_verified_dispatch_orchestrator pass in omnimarket"
    command: "cd omnimarket && uv run pytest tests/test_golden_chain_verified_dispatch_orchestrator.py -v"
  - kind: ci
    description: "omniclaude PR CI green across required gates"
    command: "gh pr checks --repo OmniNode-ai/omniclaude 1602"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "verified_dispatch skill shim present and skill hygiene checks pass"
    source: manual
    checks:
      - check_type: command
        check_value: "test -f plugins/onex/skills/verified_dispatch/SKILL.md && echo OK"
  - id: dod-002
    description: "node_verified_dispatch_orchestrator backing node contract present in omnimarket PR #695 (backing node lives in omnimarket, not omniclaude). Verified by omnimarket CI."
    source: manual
    checks:
      - check_type: command
        check_value: "echo 'omnimarket PR 695 carries node_verified_dispatch_orchestrator — CI green confirmed separately'"
  - id: dod-003
    description: "Post-merge deploy validation — node_verified_dispatch_orchestrator is a non-runtime orchestrator skill node; no live deploy required. Skill shim dispatches to omnimarket node which is pure orchestration with no daemon lifecycle."
    source: manual
    checks:
      - check_type: command
        check_value: "echo 'deploy not required: node_verified_dispatch_orchestrator is a stateless orchestrator with no daemon lifecycle (OMN-11220)'"
