---
schema_version: "1.0.0"
ticket_id: "OMN-12391"
title: "Reconcile unused omniclaude skill helper exports and consumers"
summary: >
  Prior verification confirmed three dead exports in omniclaude skill helpers
  (get_recent_message_count, get_consumer_groups in kafka_helper.py; execute_query in
  db_helper.py) and identified live surfaces that static analysis mis-flagged as dead.
  This ticket removes the confirmed-dead exports and annotates live surfaces so future
  sweeps do not re-flag them.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1700"
    description: >
      omniclaude PR #1700 removes get_recent_message_count, get_consumer_groups
      (kafka_helper.py) and execute_query (db_helper.py); adds KEEP guard comments to
      get_correlation_id, cmd_init, cmd_end, and cmd_set_active_run. Unit test suite:
      5048 passed, 5 skipped (4 pre-existing failures on dev baseline, confirmed
      pre-existing). All pre-commit hooks pass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12391/dod-omniclaude-pr-1700/command.yaml\""
  - id: "dod-omniclaude-pr-1700-deploy-gate"
    description: >
      Runtime-path changes are limited to topic-lint suppressions and no runtime deploy
      is required before merge; post-merge runtime deploy and manual integration sweep
      remain required release evidence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy gate reviewed: runtime topic-lint-only change; deploy validation scheduled post-merge"
  - id: "dod-occ-pr-1839"
    description: >
      onex_change_control PR #1839 carries the central OMN-12391 contract and receipts
      for omniclaude PR #1700.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12391/dod-occ-pr-1839/command.yaml && grep -q '^pr_number: 1839$' drift/dod_receipts/OMN-12391/dod-occ-pr-1839/command.yaml"
