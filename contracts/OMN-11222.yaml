---
schema_version: "1.0.0"
ticket_id: OMN-11222
title: "Add demo_readiness_team skill shim"
summary: "Add the demo_readiness_team skill shim and generated node_skill_demo_readiness_team_orchestrator shell."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omniclaude PR #1605 CI gates pass after repo-local deploy contract evidence is present."
    command: "gh pr checks 1605 --repo OmniNode-ai/omniclaude"
  - kind: manual
    description: "The generated skill orchestrator is dispatch-only and does not perform live deploys or production mutation."
    command: "grep -R 'NEVER' plugins/onex/skills/demo_readiness_team/SKILL.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-skill-boundary
    description: "The demo readiness skill is a dispatch-only shim with an explicit production authority boundary."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -R 'NEVER' plugins/onex/skills/demo_readiness_team/SKILL.md && grep -q 'node_type: ORCHESTRATOR_GENERIC' src/omniclaude/nodes/node_skill_demo_readiness_team_orchestrator/contract.yaml"
  - id: dod-deploy-validation-surface
    description: "No live deploy was performed for this PR; post-merge deploy validation is recorded for the generated orchestrator shell."
    source: manual
    checks:
      - check_type: command
        check_value: "post-merge deploy validation: after user-authorized deploy, verify node_skill_demo_readiness_team_orchestrator contract parses and the demo_readiness_team shim dispatches only to omnimarket demo readiness nodes"
