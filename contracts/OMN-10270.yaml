---
schema_version: "1.0.0"
ticket_id: OMN-10270
summary: "Align delegate command payload with the delegation orchestrator contract"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR checks pass, including deploy gate for runtime-adjacent model changes"
    command: "gh pr checks 1461 --repo OmniNode-ai/omniclaude --watch"
  - kind: "manual"
    description: "Delegate runtime-backed skill path validates as an explicit event-bus command producer"
    command: "python scripts/validation/validate_runtime_backed_skill_paths.py --skill delegate --strict --report"
  - kind: "manual"
    description: "Delegate publisher tests validate the emitted ModelEventEnvelope and ModelDelegationCommand payload"
    command: "uv run python -m pytest tests/unit/hooks/lib/test_delegate_kafka_publish.py tests/unit/node_delegation_orchestrator/test_model_delegation_command.py tests/validation/test_validate_runtime_backed_skill_paths.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Delegate runtime-backed skill path validator passes in strict mode"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python scripts/validation/validate_runtime_backed_skill_paths.py --skill delegate --strict --report"
  - id: "dod-002"
    description: "Delegate command producer and delegation command model tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -m pytest tests/unit/hooks/lib/test_delegate_kafka_publish.py tests/unit/node_delegation_orchestrator/test_model_delegation_command.py tests/validation/test_validate_runtime_backed_skill_paths.py -q"
  - id: "dod-003"
    description: "Runtime-adjacent delegation command schema change has a deploy verification surface before merge"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy verification: gh pr checks 1461 --repo OmniNode-ai/omniclaude --watch"
