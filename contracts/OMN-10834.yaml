---
schema_version: "1.0.0"
ticket_id: OMN-10834
title: "Replace delegation Unix socket transport with contract-driven Kafka bus"
summary: "Delete transport.py and daemon_fallback.py; rewrite delegate skill fallback
  to publish directly to onex.cmd.omniclaude.delegate-task.v1 via confluent_kafka.Producer;
  add transport_type: kafka to node_delegation_orchestrator contract.yaml."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - events
evidence_requirements:
  - kind: tests
    description: "Unit tests for Kafka transport path pass"
    command: "uv run pytest tests/delegation/test_kafka_transport.py -v"
  - kind: ci
    description: "All CI gates green on omniclaude PR #1562"
    command: "gh pr checks 1562 --repo OmniNode-ai/omniclaude"
  - kind: manual
    description: "Dead code deleted: transport.py and daemon_fallback.py absent"
    command: "test ! -f src/omniclaude/delegation/transport.py && test ! -f src/omniclaude/delegation/daemon_fallback.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-kafka-transport-tests
    description: "Unit tests for Kafka transport path pass"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/delegation/test_kafka_transport.py -v"
  - id: dod-dead-code-deleted
    description: "transport.py and daemon_fallback.py deleted; delegation __init__ cleaned"
    source: manual
    checks:
      - check_type: command
        check_value: "test ! -f src/omniclaude/delegation/transport.py && test ! -f src/omniclaude/delegation/daemon_fallback.py"
  - id: dod-contract-transport-type
    description: "node_delegation_orchestrator contract.yaml declares transport_type: kafka"
    source: manual
    checks:
      - check_type: grep
        check_value: "transport_type: kafka"
  - id: dod-deploy-kafka-topic
    description: "Delegate skill publishes to onex.cmd.omniclaude.delegate-task.v1 via Kafka"
    source: manual
    checks:
      - check_type: command
        check_value: "rpk topic produce onex.cmd.omniclaude.delegate-task.v1 --brokers 192.168.86.201:19092"
  - id: dod-pre-push-hooks
    description: "All pre-push hooks passed on omniclaude PR #1562 including deploy gate"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr checks 1562 --repo OmniNode-ai/omniclaude | grep -v fail"
