# autopilot retired prompt

This skill is retired and is not user-invocable.

Use `/onex:session` for session management and close-out orchestration. Do not
execute the retired autopilot prompt or recreate close-out orchestration inline.

Legacy flags retained in SKILL.md for metadata parity only: `--mode`,
`--autonomous`.
