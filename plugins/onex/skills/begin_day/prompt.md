# begin-day retired prompt

This skill is retired and is not user-invocable.

Use `/onex:session --mode interactive` for morning session startup, health
checks, and priority selection. Do not execute the retired begin-day prompt,
spawn probe teams, or recreate morning orchestration inline.
